---
import { AccountSettings } from '@aws-amplify/ui-react';
import MainLayout from "../../../layouts/main.astro";
import AccountSettingsWrapper from "../../../components/account/account-settings-wrapper";
import PasswordChangeForm from "../../../components/account/password-change-form";
import AuthView from '../../../components/AuthView';
import '@aws-amplify/ui-react/styles.css';

---
<AuthView client:load>
<MainLayout title={`Berklee Open: Update Password`} showHeader={true} cssClass="account">
  <AccountSettingsWrapper client:load currentPage="/account/password">
    <div class="flex gap-20">
      <div class="w-full account">
        <h1 class="text-2xl font-bold mb-6">Update Password</h1>
        <PasswordChangeForm client:load />
      </div>
    </div>
  </AccountSettingsWrapper>
</MainLayout>
</AuthView>